<%- entities.map(entity => `import { Module } from '@nestjs/common';\nimport { ${entity.name}Service } from './${entity.name.toLowerCase()}.service';\nimport { ${entity.name}Controller } from './${entity.name.toLowerCase()}.controller';\n\n@Module({\n  providers: [${entity.name}Service],\n  controllers: [${entity.name}Controller],\n})\nexport class ${entity.name}Module {}`) .join('\n\n') %>
